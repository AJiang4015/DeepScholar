# 导学-深度研搜

> 对应简历项目：《多智能体深度研究系统》（深度研搜 / DeepScholar / deepsearch-agents）
> 本文件项目根 = 仓库 `E:\CodeField\deepsearch-agents`（git 分支 main）。
> ⚠️ 事实口径：R4 任务运行时（`app/runtime/task_manager.py`、`tests/test_task_manager.py`、`tests/test_task_api.py`、`docs/spec/2026-09-03-R4-agent-task-runtime.md`）目前是**工作区未提交改动**（`git status` 可见 untracked），README 尚未同步；学习时以工作区代码为准，README 仅作旧版对照。所有路径均为相对项目根的路径。

---

## 1. 前置知识（面试高频标注）

| 知识点 | 为何需要 | 在本项目中的位置 | 高频度 |
| --- | --- | --- | --- |
| asyncio 任务与取消：`CancelledError`、`wait_for`、`shield`、done 回调、`asyncio.Queue` | 长任务后台执行、协作取消、宽限收敛、per-thread 有序投递全部建立在这套语义上 | `app/runtime/task_manager.py`、`app/api/monitor.py` | 高 |
| 状态机建模：状态集、合法转换表、终态集合、纯函数判定 | TaskStatus 五态与"非法转换约束 / 终态 exactly-once 收敛"直接对应 | `task_manager.py` 顶部 `TaskStatus / _LEGAL_TRANSITIONS / can_transition` | 高 |
| ContextVar 与 Token reset（协程级上下文，区别于 ThreadLocal / 全局变量） | 会话身份（thread_id / session_dir / run_id）在异步与线程池边界下不串台 | `app/api/context.py`、`app/api/monitor.py`（run_id） | 高 |
| LangGraph Checkpoint 语义：`checkpointer` 的 a* 方法、thread_id 索引、`astream` 驱动 | 理解 SQLite Checkpointer 为什么需要"异步桥接"、重启恢复能恢复什么 | `app/agent/main_agent.py`、`app/runtime/checkpoint.py` | 高 |
| DeepAgents / Agent 工具调用：`create_deep_agent`、subagents、description 路由、工具调用在流式输出中的形态 | Orchestrator–Workers 编排与 `task` 子智能体调用识别是项目入口 | `app/agent/main_agent.py`、`app/agent/subagents/*`、`app/prompt/prompts.yml` | 高 |
| SQL 注入与"词法级只读白名单"的思路、参数绑定、纵深防御 | Text-to-SQL 工具层的 fail-closed 边界是本项目安全叙事核心 | `app/utils/sql_security.py`、`app/tools/db_tools.py` | 高 |
| 路径安全：`resolve()`、`is_relative_to`、目录穿越、文件名净化、白名单 | 模型可控路径/文件名是"新的不可信输入"，这是 Agent 安全区别于传统后端的点 | `app/utils/path_utils.py`、`app/utils/upload_guard.py`、`app/utils/session_id.py` | 中 |
| WebSocket 事件推送：loop 亲和、per-thread 队列 + 单消费者、`call_soon_threadsafe` | 事件按会话串行有序到达、跨 async/sync 边界不丢序 | `app/api/monitor.py` ConnectionManager | 中 |
| FastAPI：后台任务、`lifespan`、UploadFile、TestClient | API 装配层与确定性集成测试的载体 | `app/api/server.py`、`tests/test_task_api.py` | 中 |
| pytest-asyncio / skipif 门控 / 假 runner 注入 | 状态机与取消竞态的确定性测试策略 | `tests/*`、`TESTING.md` | 中 |
| ReportLab 中文字体（STSong-Light 等）与 Markdown 子集解析 | 中文 PDF 交付的实现路径（了解即可） | `app/utils/word_converter.py`、`app/tools/pdf_tools.py` | 低 |

## 2. 重点亮点与学习顺序（先看这个）

| 亮点标题（通用表达） | 为什么重要 | 通用技术关键词 | 先看哪些文件 | 建议学习顺序 |
| --- | --- | --- | --- | --- |
| 显式任务生命周期状态机 | 简历 bullet 2 核心：把"任务状态"做成 Runtime 的权威事实源，修复"吞错即成功" | 状态建模、纯函数判定、终态 Exactly-Once、存储与活跃对象分离 | `app/runtime/task_manager.py` 全文；`tests/test_task_manager.py`；`docs/spec/2026-09-03-R4-agent-task-runtime.md` | 1 |
| asyncio 协作取消与替换一致性 | 简历 bullet 3 核心：`wait_for(shield(task), grace)`、宽限超时后 done 回调最终化、旧任务不覆盖新记录——最容易被深挖的并发语义 | 取消窗口、shield、done 回调、身份校验、无后台轮询 | `task_manager.py` 的 `submit / cancel / _finalize_from_done`；`tests/test_task_manager.py`（CancelledError 计数断言） | 2 |
| 统一运行时事件模型与有序投递 | 简历 bullet 4 前半：`event_id / run_id / thread_id / seq` 信封、终态一次性守卫、fail-open 观测 | 事件模型、分配全序 vs 时间戳、per-thread 队列、观测与主链路解耦 | `app/api/monitor.py`（RunTerminalGuard / ToolMonitor / ConnectionManager）；`app/api/context.py`；`tests/test_agent_events.py` | 3 |
| 执行状态持久化与进程重启恢复 | 简历 bullet 4 后半：内存 Checkpoint → 文件型 SQLite，含官方 a* 方法实测缺陷与异步桥接 | Checkpoint、异步/同步桥接、fail-fast、恢复边界 | `app/runtime/checkpoint.py` 全文；`tests/test_checkpoint_recovery.py`；`app/agent/main_agent.py` 中 checkpointer 装配处 | 4 |
| Tool Calling 副作用 Fail-Closed 治理 | 简历 bullet 5：路径 / 文件名 / thread_id / Text-to-SQL 四类模型可控输入全部 fail-closed，纯函数化可单测 | 不可信输入、路径沙箱、白名单、负路径对抗矩阵 | `app/utils/path_utils.py`、`upload_guard.py`、`session_id.py`、`sql_security.py`、`app/tools/db_tools.py`；对应 `tests/test_*`；`docs/problem/P001–P004` | 5 |
| 多智能体编排与角色化工具归口 | 简历 bullet 1 + 项目定位：主智能体只持文件交付工具，检索工具按专家角色分挂，description 参与路由 | Orchestrator–Workers、工具面收敛、上下文隔离 | `app/agent/main_agent.py`、`app/agent/subagents/*`、`app/prompt/prompts.yml`、`app/api/server.py` | 6（入口先扫一遍即可） |

学习顺序建议：**先跑通主链路**（README 快速开始 + 一条任务示例）→ 按上表 1→6 精读；每个模块都"实现 + 它的测试 + 它的 spec/problem 记录"三者一起看。

## 3. 必备知识点（精简 checklist）

- [ ] `TaskStatus` 五态与**无 pending** 的设计原因（无队列、提交即运行、同 thread 替换语义）。
- [ ] `can_transition` 是纯函数；终态集合不可再转换；`_apply_transition` 对非法/已终态转换直接忽略。
- [ ] `submit` 的替换语义：旧记录先置 `CANCELLED(replaced)` 并让 `_records` 立即指向新 run；旧 task 只收到一次 `cancel()`。
- [ ] `cancel` 为何是 `async`；`await asyncio.wait_for(asyncio.shield(task), grace)` 三行代码各自在防什么（一次取消 / 宽限 / 防二次取消）。
- [ ] 宽限超时返回 CANCELLING 后，谁把任务最终化成 CANCELLED？（done 回调 `_finalize_from_done`，不是后台任务/轮询。）
- [ ] 旧 task 迟到回调为何不会覆盖新记录？（identity 校验：`_tasks.get(tid) is task`；否则只消费异常。）
- [ ] `_tasks`（活跃 task，终态即清理）与 `_records`（latest 记录，终态后仍保留、`status()` 唯一数据源）分离的意义。
- [ ] `RunOutcome(terminal, detail)` 与 `_outcome_to_terminal` 的映射；"run 抛异常"与"run 返回 error 终态"两条路径如何都收敛成 FAILED；CancelledError 路径为何是 CANCELLED。
- [ ] 事件信封字段与 `seq` 语义：**分配全序**（emit 时锁内自增），排序以 seq 为准、timestamp 不参与。
- [ ] `RunTerminalGuard`：task_result / task_cancelled / error 三类终态事件 per-run 至多一次（`may_emit` 的副作用式占用）。
- [ ] per-thread `asyncio.Queue` + 单消费者：同一会话到达顺序 == 入队顺序；不同会话互相独立。
- [ ] 跨线程投递：同循环 `create_task` vs 跨线程 `call_soon_threadsafe`；monitor fail-open（投递失败只影响可观测性）。
- [ ] ContextVar 三件套（thread_id / session_dir / run_id）的 set / get / reset（Token），为什么 executor 线程里能读到（LangChain 提交任务时复制调用方上下文——已实测）。
- [ ] `AsyncBridgeSqliteSaver`：官方同步 `SqliteSaver` 的 a* 方法抛 `NotImplementedError`（两套环境实测），子类用 `asyncio.to_thread` 桥接；同步方法原样继承。
- [ ] Checkpoint 边界：持久化图状态/消息；外部副作用不自动重放；不是 BaseStore / 长期记忆；DB 不可写时 fail-fast 抛错、绝不静默退化内存版。
- [ ] 工具安全四防线：`resolve_path`（会话目录包含性，越界抛 `PathSafetyError`）、上传治理（扩展名白名单 / 大小上限 / 文件名净化 / 随机存储名 + manifest）、`thread_id` 安全字符集、SQL 只读（语句类型 + 表名双重白名单 + 参数绑定 + 审计）。
- [ ] `sql_security` 词法器跳过字符串/注释，防止"注释内分号/关键字"误判；WITH/派生表宁拒勿放；白名单数据源是 `list_sql_tables` 动态发现的表集合。
- [ ] 提示词只给模型**相对路径**工作目录指令，模型侧虚拟路径/嵌套路径由 `resolve_path` 清洗——"模型不可信"落在哪一层要能说清。

## 4. 推荐阅读（结合仓库）

> 阅读位置均相对项目根（仓库 `E:\CodeField\deepsearch-agents`）。R4 相关为工作区未提交改动。

| 主题 | 通用技术点 | 建议阅读位置 | 预计时间 | 读完能回答什么 |
| --- | --- | --- | --- | --- |
| 项目总览与链路 | 端到端主链路、快速开始 | `README.md`（工作目录与产物：`app/output/`、`app/updated/`） | 30–40 min | 项目做什么、怎么跑、四路信息源与文件交付如何串起来 |
| 系统架构与工程边界 | 组件划分、安全/凭据红线 | `ARCHITECTURE.md`；架构图 `docs/images/deepsearch-system-architecture.svg` | 20–30 min | 模块职责边界、为什么某些功能不做（对照 ROADMAP） |
| API 装配层 | FastAPI 后台任务、WS、上传、下载 | `app/api/server.py`（lifespan / 6 个端点 / TaskManager 装配） | 30 min | 任务提交/取消/状态/上传/下载/WS 六个接口如何协作 |
| 主智能体执行入口 | DeepAgents astream、上下文、终态返回 | `app/agent/main_agent.py`（`run_deep_agent`）；`app/prompt/prompts.yml`；`app/agent/subagents/*` | 30–40 min | run_id 如何贯通、子智能体调用如何识别、会话目录与上传还原流程 |
| 任务生命周期运行时（本项目最核心） | 状态机、取消语义、替换一致性 | `app/runtime/task_manager.py` 全文 + `tests/test_task_manager.py` + `docs/spec/2026-09-03-R4-agent-task-runtime.md` | 60–90 min | 五态设计取舍、shield 防二次取消原理、done 回调最终化、旧 task 覆盖防护 |
| 事件模型与有序投递 | 统一信封、终态守卫、per-thread 队列 | `app/api/monitor.py` 全文 + `app/api/context.py` + `tests/test_agent_events.py` | 45–60 min | seq 为什么是分配全序、如何保证会话内有序、fail-open 取舍 |
| Checkpoint 持久化 | SQLite Checkpoint、a* 桥接、重启恢复 | `app/runtime/checkpoint.py` + `tests/test_checkpoint_recovery.py` + `docs/spec/2026-09-03-R2-persistent-checkpoint.md` | 45–60 min | 为什么官方同步 SqliteSaver 配不上 astream、AsyncBridge 思路、fail-fast 语义、恢复边界 |
| Text-to-SQL 安全 | 只读白名单、词法解析、参数绑定 | `app/utils/sql_security.py` 全文 + `app/tools/db_tools.py` + `tests/test_db_tools.py` + `docs/problem/P001-sql-tool-write-sql.md` | 45–60 min | 校验链各步骤、绕过面（多语句/注释/大小写/OUTFILE）、CTE 为何拒绝、审计记什么 |
| 文件与上传安全 | 路径沙箱、上传治理、会话标识净化 | `app/utils/path_utils.py`、`upload_guard.py`、`session_id.py` + `tests/test_path_utils.py`、`tests/test_upload_guard.py`、`tests/test_session_id.py` + `docs/problem/P002/P003/P004` + `docs/spec/2026-09-03-R1-file-security.md` | 45–60 min | 每个攻击面（../、绝对路径、畸形文件名、超限、thread_id 穿越）fail-closed 在哪一层生效 |
| 前端事件消费（了解即可） | WS 事件流驱动 UI | `frontend/src/hooks/useDeepAgentSession.ts`（可 glob 确认最新路径） | 20 min | 前端如何按 thread 收事件、任务状态展示、文件列表刷新 |
| 测试与验证契约 | 确定性测试、双环境回归 | `tests/*`（9 个文件）+ `TESTING.md` + R4 spec 的验收章节 | 30–45 min | 状态机/取消/替换为什么用假 runner 可确定性测试、skipif 门控原则 |
| 演进记录（可选） | Spec/Decision 驱动的工程化 | `docs/spec/2026-09-03-R*.md`、`docs/ROADMAP.md`、`docs/problem/P001–P006`、`DECISION.md`（如 D004/D007/D009 相关条目） | 45 min | R1→R4 每轮"问题→设计→实现→验证"如何闭环 |

## 5. 自学提醒

本文件只负责给出学习路径、阅读顺序与题目，**不提供逐行讲解**。若某文件或原理看不懂（例如 `shield` 与 `wait_for` 的取消对象、`seq` 全序的推理、checkpoint 的 a* 桥接为什么必要），请继续追问 AI，把它讲到能用第一人称复述为止。每读完一个模块，尝试不看代码默写它的输入输出与关键取舍，再对照源码。

## 6. 项目技术定位

**定位：AI × 后端工程（交叉，偏 LLM Application → Agent Runtime 方向）。**
依据：主链路是 DeepAgents/LangChain 驱动的多智能体编排与 Tool Calling（AI 应用形态），但真正沉淀的复杂度在 asyncio 任务生命周期、可观测事件模型、持久化与工具副作用治理（Agent Runtime / Reliability 工程能力），后者对 LLM 应用后端与 Agent Engineer 岗位的匹配度最高；同时含 FastAPI/WebSocket/MySQL 等后端工程要素，可向通用后端岗位讲述。

## 7. 核心原理解析（问题 → 机制 → 在本项目中的落点）

1. **任务状态为什么需要"显式账本"而非裸 Task？**
   问题：原实现 `dict[thread_id] → asyncio.Task`，run 内部异常被吞（只发 error 事件不 re-raise），服务端分不清 succeeded / failed；取消超宽限后状态不再收敛，前端只能靠 WS 事件猜。
   机制：五态状态机 + 合法转换表（纯函数）+ TaskRecord（thread_id/run_id/status/时间戳/terminal/detail）+ RunOutcome 返回终态；"活跃 task 表"与"latest 记录表"分离。
   落点：`task_manager.py`；`status()` 只读 `_records` → 终态后仍可查，成为前端自愈的权威数据源。

2. **asyncio 协作取消如何做到"只取消一次、宽限超时不误杀、最终必然收敛"？**
   问题：`task.cancel()` 注入 CancelledError 是协作式；宽限内没结束怎么办？直接 `wait_for(task)` 超时路径会再 cancel 一次原任务（二次取消）；引入监视任务轮询又脏。
   机制：`await asyncio.wait_for(asyncio.shield(task), grace)`——shield 保护原 task，wait_for 超时取消的是外层 shield future；宽限超时返回 CANCELLING；原 task 真正结束时其自身 done 回调 `_finalize_from_done` 收敛为 CANCELLED；无后台任务/轮询（测试断言原任务 CancelledError 计数 == 1）。
   落点：`TaskManager.cancel()` + `_finalize_from_done`；`tests/test_task_manager.py`。

3. **同一会话重复提交/旧任务迟到回调，如何保证不串状态？**
   问题：同 thread 新 run 启动后，旧 run 的迟到 done 回调可能把新记录改成终态。
   机制：submit 时旧记录立即 `CANCELLED(replaced)` 并让 `_records` 指向新 run（旧 run 自此不可再查）；`_finalize_from_done` 做 identity 校验（`_tasks.get(tid) is task`），不匹配只消费异常、不改任何记录。
   落点：`TaskManager.submit` / `_finalize_from_done`（v3 语义）。

4. **异步长任务的事件怎么做到"同一会话有序、可关联、可排序、不拖垮主链路"？**
   问题：工具调用、子智能体调用、任务生命周期事件来自不同协程甚至线程池线程，缺少统一 schema 与身份；多会话并发时若全局串行发送会互相阻塞。
   机制：统一信封（event_id/run_id/thread_id/seq），seq 在锁内自增（进程内分配全序，排序以 seq 为准）；投递按 thread 建 `asyncio.Queue` + 单消费者（到达顺序 == 入队顺序）；executor 线程内 emit 依赖 LangChain 提交时复制调用方 ContextVar（已实测）；任何投递异常 fail-open（只影响可观测性）。
   落点：`monitor.py` ToolMonitor / ConnectionManager；终态事件由 `RunTerminalGuard` per-run exactly-once。

5. **"进程重启后还能接着跑"与"memory checkpoint"的差距在哪？**
   问题：InMemorySaver 重启即丢；官方同步 SqliteSaver 的 a* 异步方法与 `astream` 不兼容（实测抛 NotImplementedError）。
   机制：自实现 `AsyncBridgeSqliteSaver`（a* 桥接到 `asyncio.to_thread` 的同步实现，同步方法原样继承）；DB 路径 env 可配、进程内按路径单例；eager `setup()` 即真实写盘一次，失败 fail-fast 抛错。
   落点：`checkpoint.py`；恢复边界（图状态/消息可恢复、外部副作用不自动重放）用真实子进程测试验证（`tests/test_checkpoint_recovery.py`）。

6. **为什么多智能体系统的安全边界与普通后端不同？**
   问题：模型成为新的不可信输入源——LLM 生成的路径、上传文件名、thread_id、SQL 一旦直接进入文件系统/数据库即可越界；靠提示词约束不可验证。
   机制：工具层 fail-closed（非法即拒绝而非放行）+ 纵深：路径沙箱（会话目录包含性校验）、上传治理（白名单/大小/净化/随机存储名+manifest）、会话标识净化、SQL 只读（语句类型 + 表名双重白名单 + 参数绑定 + 审计）；校验逻辑纯函数化以便无外部依赖单测，配负路径对抗矩阵。
   落点：`app/utils/*` 与 `app/tools/db_tools.py`；P001–P004 高危问题修复记录。

## 8. 关键设计决策（备选 / 取舍 / 风险 / 验证）

| 决策 | 备选方案 | 取舍 | 风险 | 验证 |
| --- | --- | --- | --- | --- |
| 每 thread 单活跃任务 + 替换语义（无队列、无 pending） | 任务队列 + 排队执行 | 提交即运行、语义简单、杜绝同会话并发写目录 | 新任务顶掉旧任务（用户需接受） | `tests/test_task_manager.py` replacement 用例 |
| `cancel` 用 `wait_for(shield(task), grace)`，宽限可配（默认 1s） | 无宽限直接判死 / 立即二次强杀 | 给协作取消留出收敛窗口 | 宽限内未结束 → 先返回 CANCELLING | 取消竞态测试（CANCELLING→SUCCEEDED/FAILED 合法） |
| 终态收敛放在任务自身 done 回调 | 后台监视协程轮询 | 无额外资源、天然绑定任务结束 | 需身份校验防旧任务覆盖 | 断言"无新增后台 task"的测试 |
| `_tasks`/`_records` 双表分离 | 单一 dict 存任务+状态 | 状态查询与活跃任务解耦 | 略增内部不变量 | status API 测试（终态后仍可查） |
| 观测链路 fail-open（投递失败只打日志） | fail-closed（投递失败阻断执行） | 可观测性不影响主业务 | 事件可能丢失（无持久化，R5 再做） | `tests/test_agent_events.py` |
| 事件 `seq` 为进程内分配全序 | 时间戳排序 | 无时钟歧义、可精确定序 | 单进程语义，分布式中立需扩展 | seq 单调断言测试 |
| Checkpoint 用文件型 SQLite + a* 桥接，fail-fast | 退回 InMemorySaver / 自写轮询落盘 | 零额外部署、保持久化承诺 | 单机语义，多实例需 Postgres（未做） | 真实子进程 restart recovery 测试 |
| SQL 安全用"词法级白名单 + 参数绑定"，工具层强制 | 依赖提示词自律 / 上完整 SQL parser | 可测、fail-closed、宁拒勿放 | 词法启发式非 100% 覆盖合法 SQL | 86 项 SQL 安全单测 + 真实 MySQL 集成（skipif） |
| 模型只见相对路径工作目录 + `resolve_path` 出口校验 | 信任模型绝对路径 | 双重约束、防虚拟路径/嵌套路径 | 提示词与工具间语义需一致 | `tests/test_path_utils.py` 负路径矩阵 |
| 多智能体编排沿用 DeepAgents Orchestrator–Workers | 单 Agent 全工具 / 自研编排内核 | 复用成熟执行器，专注 Runtime 治理 | 依赖生态（DeepAgents/LangGraph 版本） | 主链路 E2E + `test_task_api.py` 注入假 astream |

## 9. 量化与验证（含待测）

- 已落地并留档（以仓库记录为准，面试前可重跑验证）：
  - 上传/文件安全治理配套 80+ 项单元测试（README 记录）；
  - SQL 只读安全配套 86 项测试（README 记录；校验层纯函数化，无 DB 即可全量跑）；
  - R4 交付验收记录：系统 Python 环境 209 passed / 8 skipped，uv 锁定环境 220 passed / 0 skipped（含真实 MySQL 集成）——该数字出自仓库内简历补充稿的验收留档，**建议面试前在仓库实际执行 `uv run pytest -q` 复核一次**，并留意外部依赖用例的 skipif 行为。
- 待测/如实标注项（面试不要当成果讲）：
  - 任务取消对**同步阻塞型第三方调用**是"尽力而为"：协程层 CancelledError 已收敛，但 executor 线程里的同步调用不可中断，取消可能延迟生效（代码注释与 R4 spec 已如实记录）；
  - 事件流目前只到 WebSocket，未落库（历史回放是 R5 方向）；
  - 未做压测与生产观测，无 QPS / 延迟 / Token 指标；
  - 单机 SQLite checkpoint，多实例共享需 Postgres（未做）；
  - 真实 LLM 下的 cancel/替换端到端行为受本机环境限制（API 层用假 runner/假 astream 做确定性验证，未打真实 LLM 做 E2E 的部分如实标注）。
