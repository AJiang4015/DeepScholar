# 面经-深度研搜

> 对应简历项目：《多智能体深度研究系统》（深度研搜 / DeepScholar / deepsearch-agents）
> 项目根 = 仓库 `E:\CodeField\deepsearch-agents`。正文以通用工程语言为主，内部符号一律收进文末"源码证据索引"。
> 事实口径：R4 任务运行时为工作区**未提交**代码（`task_manager.py` 等 untracked）；所有指标来自仓库验收留档，非线上数据；面试前请用 `uv run pytest -q` 复核测试数量。

---

## 1. 项目简介（简历可用）

面向深度研究场景构建的多智能体研究系统：主智能体负责任务理解、研究规划与智能路由，协同 Web 检索、结构化数据查询、私有知识库问答等专家智能体完成多源信息获取与交叉验证，并统一生成 Markdown / PDF 研究报告。项目不止于一次 LLM 调用，而是把多智能体协作工程化为带显式任务生命周期、状态持久化与恢复、运行时可观测事件模型、工具副作用 Fail-Closed 治理的 Agent Runtime。

## 2. 简历 bullet（6 条，可直接选用）

> 每条只表达一个支柱，均为"问题或演进 → 机制 → 结果"结构；无虚构指标。

**显式任务生命周期治理：** 针对长任务原先只登记为后台协程、内部异常被吞、服务端无法区分成功与失败的问题，设计五态任务状态机（运行中 / 取消中 / 成功 / 失败 / 已取消）与独立的任务记录对象（含会话、批次、状态、时间戳与终态详情），转换合法性做成可单测的纯函数并约束终态只能收敛一次；配套只读状态查询端点，使"任务成败"成为服务端权威、可查询的事实，调用方不再依赖事件流间接猜测。

**异步长任务的可靠取消与替换：** asyncio 取消是协作式的，直接强杀会造成二次取消或状态悬空。实现异步取消接口：先注入一次取消请求，再用超时包装被保护的原任务作为取消窗口——宽限超时取消的是外层包装而非任务本体；超时后任务真正结束由它自身的完成回调收敛为"已取消"，全程不引入后台监视与轮询；同一会话重复提交时旧批次先置为"已替换"，其迟到的完成回调经身份校验后绝不覆盖新批次状态。

**运行时可观测性（统一事件模型）：** 把工具调用、子智能体调用、任务生命周期统一为带事件标识 / 批次标识 / 会话标识 / 序号的事件信封，序号是进程内分配全序（排序以序号而非时间戳为准）；同一会话的事件经独立队列 + 单消费者串行投递，到达顺序与产生顺序一致；三条终态事件（成功 / 取消 / 异常）在同一批次内至多发出一次；观测链路整体 fail-open——事件投递失败绝不反向破坏智能体主执行。

**执行状态持久化与进程重启恢复：** 将执行状态从进程内存升级为文件型 SQLite 持久化，以会话标识索引 LangGraph 图执行状态；实测发现官方同步持久化器的异步接口抛未实现异常、与异步流式执行不兼容，据此做异步桥接子类，让同一实例同时支撑异步生产路径与同步恢复路径；数据库不可写时立即失败并报错，绝不静默退回内存模式；用真实子进程验证"写入检查点 → 新进程打开同一库续跑"。

**工具副作用 Fail-Closed 治理：** 多智能体场景下模型是新的不可信输入源。对模型可控的读写路径、上传文件名、会话标识与 Text-to-SQL 输入在工具层建立拒绝式校验：路径解析统一出口并做会话目录包含性校验，上传做类型白名单 / 大小上限 / 文件名净化 / 随机存储名 + 原名映射，会话标识限定安全字符集，SQL 只放行只读语句类型并对表名做白名单比对，动态值一律参数绑定执行并留审计；校验逻辑纯函数化，可无外部依赖全量单测，并以负路径对抗用例矩阵回归。

**Agent Runtime 的可验证测试体系（可并入第 5 条口播）：** 对状态机、取消竞态、同会话替换、并发隔离建立纯 asyncio 单元测试；API 控制流用注入的假执行器 / 假流式输出驱动真实入口做确定性集成测试，不打真实模型；依赖外部环境的用例以条件跳过门控，保证测试基建本身可运行、可回归（双 Python 环境验收留档，详见附录）。

## 3. 面试问题（15 主问，按出现频率从高到低）

> 每题含"考察 / 覆盖要点 / 第一人称口播（STAR）"，其后为追问。口播仅供校准语气，请用自己的真实经历改写细节。

### 第一梯队（必问）

**Q1（★★★★★）请用两分钟介绍这个项目：它解决什么问题、整体架构如何、为什么是多智能体而不是单 Agent 直接串工具？**

- 考察：表达结构、架构自洽性、技术选型的真实理由、个人边界。
- 覆盖：一句话定位 → 一主多从架构 → 数据/事件流 → 为什么需要分工与路由 → 与普通问答/单 Agent 的差别。
- 口播：我在做一个面向深度研究场景的多智能体系统，简单说就是把"用户提一句研究问题"升级成"系统自己完成规划、多来源查证、汇总并产出研究报告"的完整任务。用户的问题往往要同时查公开网络、结构化数据库和私有知识库，还要读上传的附件，最后生成 Markdown 或 PDF。单 Agent 把所有工具挂在身上，模型面对几十个工具选择压力大、权限也无法收敛；所以我采用了一个主智能体加三个专家子智能体的结构：主智能体只负责理解任务、规划步骤、把子任务路由给合适的专家并做最终汇总，它手里只有文件交付类工具；网络检索、数据库查询、知识库问答分别由三个专家负责。这样工具按角色归口，每个智能体只面对自己职责内的少量工具。执行过程会以结构化事件实时推到前端，任务的状态、取消、失败在服务端都有明确记录。我个人独立完成从编排入口到运行时各层模块的设计实现与测试，不是只写提示词那种 Demo。
- 追问 Q1a（★★★★）：如果并发用户和会话数放大一个数量级，你认为这个架构最先暴露的瓶颈是什么？为什么？
  - 口播：我会先指出这个系统当前的形态边界，再给演进路径。第一，任务运行时是单进程内的事件循环账本，状态存在进程内存里，多实例部署后任务归属和查询会失效，需要把账本落到外部存储或引入任务队列中间件。第二，事件流只推到 WebSocket，没有落库，放大后审计和回放会缺失。第三，执行状态检查点用的是单机 SQLite 文件，多实例共享必须换 Postgres 这类带并发写能力的库。第四，取消语义对同步阻塞的工具调用是尽力而为，如果工具都改成可中断或走线程池可取消的通道，取消体验会更好。我会强调我清楚这些是当前明确的单机设计边界，而不是假装它已经支持了分布式。

**Q2（★★★★★）任务状态机是简历重点。你为什么要从原来的任务登记方式重构出这套状态机？五态转换约束怎么设计，为什么没有"排队中"这个状态？**

- 考察：对原实现缺陷的识别、状态建模基本功、取舍。
- 覆盖：原实现吞异常 / 状态不可判定 / 取消后不收敛 → 状态集合与合法转换表 → 纯函数判定 → 终态 exactly-once → 无 pending 的决策链（提交即运行、无队列、同会话替换语义）。
- 口播：原实现是每个会话登记一个后台协程，接口能启动任务、能发取消，但有几个真实缺陷：协程内部如果异常，只是发一条错误事件却不向上抛出，服务端其实分不清这个任务是成功还是失败；取消请求超时后状态停在中间态，没有人再把它收敛到终态；前端只能靠事件流去猜任务到底什么状态。我重构的思路是先问"任务一生有哪些合法状态"，得到五个：运行中、取消中、成功、失败、已取消。为什么没有排队中？因为设计上同一个会话只允许一个活跃任务、提交即运行，新任务会以替换方式顶掉旧任务，根本不存在排队，加了反而误导。合法转换我用一张映射表表达，比如运行中可以去到取消中或任意终态，取消中还能收敛成成功或失败——因为协作取消下任务可能在取消请求之后才真正完成，这必须合法。终态就不可再转换，收敛由纯函数判定并统一入口执行，保证一个任务只有一个终态结果。
- 追问 Q2a（★★★★）："取消中"还能转成"成功"是不是矛盾？什么场景下会发生？
  - 口播：不矛盾，这正是协作式取消的诚实表达。任务取消只是向协程注入一个取消信号，要等协程在下一个 await 边界响应；如果代码已经越过最后会被取消的检查点，继续执行到正常返回，那么它实际上完成了。这时候如果服务端强行记成已取消，反而是错误的事实。我的实现里取消请求先把状态置为取消中，然后等待一个宽限期收敛；宽限内任务若正常完成，最终记录就是成功，取消中的状态只是"取消已被请求、结果待定"的中间事实。测试里专门构造了"收到取消但选择吞掉并正常完成"的用例，断言最终落到成功而不是已取消——我希望状态机记录的是真实发生的事，而不是请求方的意图。

**Q3（★★★★★）取消是怎么做的？`wait_for` 和 `shield` 的组合在防什么？宽限超时后谁负责收尾？**

- 考察：asyncio 取消语义细节，能否讲清"取消的是哪个对象"。
- 覆盖：task.cancel() 一次注入 → wait_for(shield(task), grace) 三件套各自作用 → 宽限超时返回 CANCELLING → 任务自身 done 回调最终化 → 无后台轮询 → 同步阻塞调用的真实边界。
- 口播：我的取消接口是异步的，核心就一句话：只发一次取消，然后用一个受保护的超时窗口等它自己收敛。第一层 task.cancel 是向任务注入取消信号，这是协作式的，必须等任务在某个 await 点响应。第二层我用带宽限的超时把任务包起来等它结束。这里最容易踩的坑是：如果直接超时等待原任务，超时路径会向原任务再取消一次，造成"二次取消"，可能打断任务正在做的正常清理；所以我把原任务用 shield 保护起来，超时到点时被取消的是外层保护壳，原任务自始至终只收到那一次取消信号。第三层是收尾责任：宽限内没结束，接口先返回"取消中"状态，等任务真正结束时，它自己的完成回调会把记录最终化为已取消——不需要任何后台监视任务或轮询，这个我在测试里用"原任务收到的取消次数必须为 1、且结束后没有新增后台任务"断言锁死。
- 追问 Q3a（★★★★）：如果任务卡在同步阻塞调用（比如线程池里的网络请求），取消还能立即生效吗？你的实现如何处理这个边界？
  - 口播：不能立即生效，这一点我在代码注释和设计文档里都如实写了。任务取消是协作式的，信号只在 await 边界生效；如果协程正等在一个线程池任务上，而那个同步调用是第三方的、不可中断的，那么取消信号要等它返回、协程重新拿到控制权之后才会被响应。所以我的语义是：协程层保证一次取消、宽限收敛、最终态必定落定，但"取消到底多快生效"取决于底层调用是否可中断，这是尽力而为。我给出的边界是两层：一是在宽限内等待、超时就返回取消中的诚实状态，而不是假装已经死了；二是如果要做生产级，工具层要提供可中断的信号通道，或者把同步调用换成真正可取消的执行单元，这是后续演进方向，我不会把当前实现说成能中断一切阻塞调用。

**Q4（★★★★★）你提到工具安全是 Fail-Closed。具体是哪些不可信输入、每个边界怎么落地？为什么不能用提示词约束替代？**

- 考察：Agent 安全心智（LLM 输出不可信）、纵深防御、工具层与提示词层差异。
- 覆盖：路径 / 上传文件名 / 会话标识 / Text-to-SQL 四类输入 → 各自的校验与 fail-closed 语义 → 纯函数化 → 为什么提示词约束不可验证。
- 口播：多智能体系统引入了一个传统后端没有的信任问题：模型会自己"生成"要访问的路径、要写入的文件名、要执行的 SQL。提示词约束本质上是一段建议，模型可能无视、可能被注入绕过，不可验证；所以我把校验下沉到工具层，语义是 fail-closed，非法输入直接拒绝而不是尝试放行再补救。四个面：第一是文件路径，模型可能给出绝对路径、虚拟路径或嵌套路径，我做了统一的路径解析出口，先规范化再校验结果必须落在当前会话目录之内，越界就抛错；第二是上传，扩展名白名单、大小上限、文件名净化，落盘用随机存储名、原名只在清单里映射，避免任何由文件名参与构造的路径逃逸；第三是会话标识，限定在安全字符集内，防止目录穿越和路由污染；第四是 SQL，只允许只读语句类型，表名与动态发现的白名单比对，值一律参数绑定执行并记录审计。这些校验全部写成纯函数，不依赖数据库和框架，所以能用对抗用例矩阵低成本地回归。
- 追问 Q4a（★★★★）：SQL 只读白名单具体怎么防绕过？如果模型写 `SELECT * FROM a WHERE 1=1; DROP TABLE b` 或者用注释包裹语句，会发生什么？
  - 口播：这正好是负路径用例的核心。校验第一步是语句类型判定：只认 SELECT、SHOW、DESC、DESCRIBE、EXPLAIN 这几种开头，而且只允许单条语句，中间出现分号直接拒绝，所以"正常语句后拼 DROP"会被整条拦下。第二步是词法级解析，会跳过字符串字面量和注释，包括行注释、块注释甚至 MySQL 的可执行版本化注释，防止模型把关键字或分号藏在注释和字符串里骗过第一个判定。第三步是表名提取，按关键字位置抓 FROM、JOIN、DESC 引用的表，再和允许表集合比对，不在白名单就拒绝。这里有个明确的取舍：解析器是词法级的，不承诺覆盖一切合法 SQL，所以遇到拿不准的写法宁可拒绝，比如带 WITH 的公共表表达式和子查询派生表我直接不支持；同时我在文档里说明，工具层只是纵深的一层，生产环境还应该配只读数据库账号兜底。测试里多语句、注释包裹、大小写绕过、SELECT INTO OUTFILE 这些都被矩阵覆盖了。

### 第二梯队（高频追问）

**Q5（★★★★）统一事件模型里 event_id / run_id / thread_id / seq 四个字段各自解决什么问题？seq 为什么不用时间戳？**

- 考察：事件建模、分布式/并发排序意识。
- 覆盖：event_id 幂等与去重基础、run_id 把"一次执行"的所有事件串起来（与任务记录同源）、thread_id 定向与会话归属、seq 分配全序；并发下时间戳不可排序。
- 口播：四个字段回答四类问题。event_id 是每条事件的唯一身份，是后续做幂等、去重和审计定位的最小单位。run_id 标识"一次执行批次"：同一个会话可能先后跑多个批次，甚至旧批次被替换，只有带批次标识才能把工具调用、子智能体调用和生命周期事件归到同一次执行上，而且它和任务记录里的批次号是同一个来源，事件层和任务层因此能对上账。thread_id 是会话归属，前端连接和事件投递都靠它定向。seq 是进程内事件分配的全序序号。为什么不用时间戳排序？因为并发场景下两个事件可能在同一毫秒产生，时间戳相等就分不出先后，还要担心时钟回拨；seq 在分配时用锁保证单调递增，事件到达顺序、展示顺序都以它为最终依据，时间戳只作展示不参与排序。这个取舍让"顺序"变得可验证。
- 追问 Q5a（★★★）：事件顺序的保证是"全局串行"吗？多会话并发时会不会互相阻塞？
  - 口播：不是全局串行，这是容易讲错的地方。顺序保证是分层的：seq 只是给事件一个全序编号，但投递不是全局单队列。每个会话有一条独立的队列配一个发送协程，事件先进本会话的队列，由这个单消费者按入队顺序串行发出，所以保证的是"同一会话内到达顺序等于产生顺序"，而且不同会话的队列彼此独立，A 会话的慢速推送不会阻塞 B 会话。跨线程的时候，比如工具跑在线程池里发事件，我用线程安全的方式把事件投回绑定的事件循环再入队，保证不丢、不乱。我当初写文档时专门把"按会话串行"和"全局单线程串行"区分开，因为面试里这两个说法差很远，后者会让系统吞吐变成所有会话共享一条链路，和实现不符。

**Q6（★★★★）你提到"失败成为第一类状态"，修复前是什么现象？修复后如何保证失败判定可靠？**

- 考察：对"吞错即成功"根因的理解、返回契约设计。
- 覆盖：原实现 run 内部异常只发事件不抛出 → 执行入口改为返回结构化的终态结果（含终态类型与详情）→ 任务层按结果映射状态 → 异常/CancelledError 两条防御路径 → 只读查询兜底。
- 口播：修复前的现象很反直觉：一个任务内部抛了异常，监控里能看到错误事件，但服务端的任务记录仍然停在运行中，甚至被当成正常结束——因为执行协程吞掉了异常，只负责发事件，没把失败告诉上层。前端拿到的要么是缺失的终态，要么是误导性的成功。我的修复分成三步。第一步是给执行入口定义一个明确的返回契约：一次执行结束时必须返回"这次执行实际发出的终态类型 + 详情"，正常结束、用户取消、内部异常都各归各位。第二步是任务层拿到这个结果后映射成最终状态：正常结束归为成功、取消归为已取消、错误归为失败，同时保留错误详情；如果协程是被取消的，取消异常那条路径也单独收敛，而不是被当成普通异常记成失败。第三步是查询兜底：任务结束后记录仍可查，任何人想知道这个会话最后一次任务成败，直接查服务端状态接口即可。测试里专门用"抛异常的执行器"验证它能落到失败态并带出异常信息，防止以后再退回吞错即成功的旧行为。
- 追问 Q6a（★★★）：如果执行入口未来忘记返回终态结果（返回了空），你的映射逻辑会怎么处理？这是防御还是掩盖？
  - 口播：我确实为这个情况留了防御分支：如果拿到的结果为空或缺少终态类型，映射逻辑默认按正常完成处理，把它归为成功，同时不会伪造终态事件名。这不是掩盖错误，而是明确的降级策略，理由是两个：第一，执行入口的契约是"总会返回终态结果"，返回空属于违反契约的异常情况，运行时已经不该再继续纠缠；第二，即便发生，成功也是比悬空更可用的状态——悬空会让调用方永远等不到结果，成功至少让链路走完且状态可查。我在防御分支旁边写了注释，说明这是防御性兜底，并且用类型契约加测试来让"返回空"本身很难发生。如果面试官继续追问，我会承认更严格的方案是让执行入口在违反契约时直接抛出、由上层记成失败，这取决于你更怕静默误报成功还是更怕状态悬空，我可以现场分析两种口径的取舍，而不是说我的选择是唯一答案。

**Q7（★★★★）同一会话重复提交任务时，你说旧任务的迟到回调不会覆盖新状态。具体是怎么做到的？**

- 考察：并发一致性的身份校验思路、内部不变量设计。
- 覆盖：提交即替换（旧批次先落"已替换"终态）→ 记录表立即指向新批次 → 完成回调身份校验（当前活跃任务是不是我）→ 双表分离（活跃任务表随终态清理、记录表保留）→ 防误清（"仍是同一个才清理"）。
- 口播：这个问题的本质是：旧任务和新任务都可能在稍后触发各自的完成回调，系统怎么知道该信谁。我的做法是把"身份"作为判断依据。同一会话再次提交时，我先做两件事：把旧批次的状态立刻置为已替换并落上终态时间，然后把记录表的指针立即指向新批次——从这一刻起，旧批次在状态查询里已经不可见了，它已经被正式替换。旧任务之后真正结束时，它自己的完成回调会先做一道身份校验：只有当当前活跃任务表中存的还是它自己时，才允许它去更新状态；否则说明它已经被替换，它唯一被允许做的事是把自己的异常消费掉避免警告，绝不允许碰记录。配套还有一个存储不变量：活跃任务表和记录表是分开的，任务一结束活跃任务就从活跃表清掉，而记录表保留到终态之后，状态查询只读记录表。这套设计保证状态机记录的是最新一次执行的真实结果，替换瞬间旧批次不可再查，避免前端看到新旧交错的状态。
- 追问 Q7a（★★★）：被替换的旧任务还在真实执行（比如正在调外部检索），此时它发的事件还会推给前端吗？会不会和新任务事件混在一起？
  - 口播：会推，但不会混。事件和任务状态是两层各自带身份：旧任务还在跑，它的工具调用、助手调用事件照常发出，但每条事件都带它自己的批次号；新任务有自己的批次号，两批事件在同一个会话流里是能区分的，前端可以按批次号把它们分开。真正被防住的是"状态覆盖"——旧任务不能把任务记录改成终态去污染新批次的状态。不过这里我也要诚实说明一个不完美的地方：同一会话下新旧任务的事件会交错出现，UI 上如果只按时间顺序渲染，用户会看到上一轮任务的工具调用突然又冒出来，体验上不干净。理想方案是前端按批次号过滤展示，或者后端在替换时给旧批次补一条"已被替换"的终态事件让前端主动收尾，这个收尾事件我当时没有做全，是已知的改进点，面试时我会主动讲出来，而不是等被追问。

**Q8（★★★★）执行状态持久化这块，为什么官方 SQLite 持久化器不能直接配你的异步执行？桥接是怎么做的？失败时为什么 fail-fast？**

- 考察：对框架内部契约的实测理解、异步/同步边界、fail-fast vs fail-open 的场景差异。
- 覆盖：LangGraph 异步执行需要检查点的异步接口；官方同步实现的异步接口实测抛"未实现"；InMemory 版能用是因为实现了异步接口；自研桥接子类用线程池执行同步实现；数据库不可写立即抛错、绝不静默退回内存版（会破坏持久化承诺）。
- 口播：这块是我真实踩坑踩出来的，不是背书。LangGraph 的异步执行要求检查点组件提供一组异步读写接口，而官方提供的 SQLite 版持久化器是同步实现，它的异步接口在真实环境里一调用就抛"未实现"异常，我在两套依赖版本上都复现过；原来项目用的内存版之所以能配合异步执行，正是因为它完整实现了异步接口。所以我写了一个桥接子类继承官方同步实现：把四个异步接口分别桥接到线程池里执行对应的同步方法，同步接口原样继承，这样同一个实例既能服务异步流式执行的生产路径，也保留同步调用和跨进程恢复的语义不变；并发安全沿用官方同步实现内部的锁，串行化对连接的访问，单进程内足够。失败语义上我选 fail-fast：初始化时就真实建表写一次盘，路径不可写或建表失败立刻抛出携带路径和原因的错误，绝不静默退回内存检查点——那等于悄悄放弃"重启可恢复"的承诺，比直接报错更危险，因为用户以为有持久化其实没有。
- 追问 Q8a（★★★★）：进程重启后到底恢复了什么？外部检索的结果、模型生成的中间内容能恢复吗？"恢复"和"续跑"边界在哪？
  - 口播：恢复的是执行状态本身，不是整个世界。LangGraph 检查点持久化的是图的状态快照，包括消息历史、节点进度和待写入的中间记录，以会话标识为索引；也就是说重启后这个会话能找回"上次跑到哪里、中间说过什么"，具备继续往下走的上下文。但外部副作用我不会声称能自动恢复或重放：网络检索请求、数据库查询、RAGFlow 调用、文件产物这些发生在系统之外，重启后由上层重新驱动才会发生，检查点层不负责重放，否则会造成副作用重复执行，这在真实系统里是要命的。所以我的表述一直是"图状态可恢复、外部副作用不自动重放"，两者是互补的：产物靠文件持久化，对话与执行进度靠检查点。测试我用了真实子进程：子进程写入检查点退出，新进程打开同一个库能找回状态续跑，不是 mock。如果面试官问更深，我会补一句这个机制和人工审批中断续跑语义是相通的，都是利用检查点把执行切段。

**Q9（★★★★）你用什么机制做会话上下文隔离？为什么不用全局变量？线程池里的工具为什么还能拿到正确的会话信息？**

- 考察：ContextVar 语义、asyncio 并发心智、跨 async/sync 边界。
- 覆盖：协程级上下文 vs 线程级 vs 全局三级对比；Token reset 防残留；线程池内能读到的原因是提交任务时复制调用方上下文（实测）；每个会话独立工作目录。
- 口播：我用的是协程级上下文变量来承载三类执行身份：会话标识、当前会话工作目录、执行批次标识。为什么不用全局变量？因为一个进程同时跑多个会话的多个任务，全局变量只有一个值，任何赋值都会串台，这是并发系统里最经典的错误。为什么不用线程本地变量？asyncio 是单线程内多协程调度，所有协程共享同一个线程，线程本地变量在协程之间根本不隔离；协程级上下文才是跟随"当前执行链"的。它的用法是入口处设置、结束处按令牌复位：一次任务开始时写入会话身份，结束后用令牌恢复原值，防止本次会话的信息残留到下一个请求。至于线程池里的工具为什么还能读到——工具如果跑在 executor 工作线程里，那是在工作线程创建时复制了调用方的上下文快照，我在真实执行路径上验证过工具里发出的事件能归属到正确的会话流，不是靠猜测。另外每个会话有自己独立的工作目录，工具读写天然按目录隔离，上下文变量负责的是身份归属，目录负责的是物理隔离，两层合起来才是完整的会话隔离。
- 追问 Q9a（★★★）：如果你把任务改成在线程池里整体执行（而不是协程），这套隔离方案还成立吗？你会怎么改？
  - 口播：如果整段执行被搬到线程池里跑，协程级上下文的优势就消失了，因为工作线程不再天然继承调用方的上下文链。这种情况我会优先考虑保持现在的形态：让执行骨架留在事件循环里，只有确需同步阻塞的个别调用才丢线程池，这也是我现在工具事件能正确归属的原因。如果业务强制要求整任务在线程池执行，改造方向有两个：一是显式传递，像请求上下文一样把会话标识作为参数贯穿到任务对象，在进入工作线程时手动设置、退出时复位；二是引入线程池专用的上下文传播机制，但要注意线程池是复用的，线程里的上下文必须每次任务前设置、任务后清理，否则下一个任务会读到上一个任务的残留，比全局变量还危险。总的来说，我的经验是"能留在协程里的就别丢线程"，把线程池当作不得不用的逃生通道，而不是默认执行场所，这样隔离与取消的语义都更简单。

**Q10（★★★）上传文件"随机存储名 + 原名映射"这套流程走一遍？为什么随机存储名本身还不够，还要在读取链路再校验一次？**

- 考察：纵深防御意识、文件链路完整性。
- 覆盖：类型白名单/大小上限/净化 → 随机存储名落盘 + 清单记录原名映射 → 任务开始时按清单还原为原名复制到会话目录（模型按原名读取语义不变）→ 读取仍走统一路径出口校验。
- 口播：上传链路我按"入口挡、存储隔离、读取再验"三层来组织。入口先做三件事：扩展名白名单校验、大小上限校验、文件名净化，把路径分隔符、点点斜杠这类字符直接处理掉，非法直接拒绝。然后落盘不用原始文件名，而是生成随机存储名，同时把"原名到存储名的对应关系"写进一个清单文件；这样用户提供的字符串永远不会参与构造磁盘路径，从根本上断掉目录穿越。任务真正开始执行前，系统读清单、把上传文件按原名复制到该会话的工作目录，让模型按它看到的原名去读取，语义不变。但随机存储名只解决了落盘路径可控，不解决"模型乱写路径"的问题，所以读取和生成的最终出口仍然要过一次统一的路径解析校验：规范化之后必须落在当前会话目录内，越界就拒绝。清单只是名字映射，不是授权边界——真正授权边界是会话目录包含性校验，这两个概念我区分得很清楚，因为面试里如果把清单说成安全机制，追问两层就会穿帮。
- 追问 Q10a（★★★）：大小上限校验如果只信请求头里的长度字段，有什么问题？你怎么做的？
  - 口播：请求头里的内容长度是客户端自报的，可以被伪造或与实际流不一致，只信它会绕过限制。我的实现是按流实际累积字节数来判断：边接收边累计，超过上限就中止接收并清理已落盘的部分，而不是等全部收完再检查。同时错误处理要保证失败后不留半截文件在会话目录里，否则既占空间又可能被后续读取链路误当作合法附件。这块的测试矩阵里就有超限中断、非法扩展名、畸形文件名这些负路径用例。如果要更严谨，生产环境还应该在网关卡一道内容长度上限做第一层，应用内做第二层，因为应用层能校验的粒度总是比网关细，但网关可以先挡住超大的恶意流防止打爆内存，两层职责不同，我当前项目里是应用层做了完整校验，网关那层属于部署环境的事。

### 第三梯队（深度区分度）

**Q11（★★★）你的系统里有"两处至多一次"：事件终态至多一次、任务状态收敛至多一次。它们的关系和区别？**

- 考察：把两层机制讲成系统而不是名词；观测层与状态层的一致性认知。
- 覆盖：事件层守卫管"发出"，状态层纯函数管"落账"；同一终态事实在两层的表达；为何需要两层而非一层；未来如何保证两层严格一致（如重放场景）。
- 口播：这是同一个"终态只能发生一次"的约束在两个层的各自落地，职责不同。事件层解决的是"发"：执行入口在成功、取消、异常三条终态事件里只能真正发出一条，我实现里是一个运行级守卫对象，它会在第一次允许某条终态事件时占用名额，之后再请求就拒绝，防止重复推送或成功取消两条都发。状态层解决的是"记"：任务记录的状态只能收敛一次，终态不可再转换，非法转换请求直接被纯函数判定拦掉。两者的关系是同一事实在两条链路上的投影：执行入口先决定这次运行到底算什么（正常完成、被取消还是异常），把决定同步给事件层去发、给状态层去记。为什么需要两层而不是只信一层？因为事件是实时推送、可能丢失或重复，状态是服务端可查询的账本，用途不同。我也承认一个未闭环的点：这两层目前靠的是同一来源各自消费，还没有一套跨层对账机制在重放场景下做严格一致性校验，这是如果要演进到事件落库、断线回放时必须补的设计，我不会把当前实现说成已经做到强一致。
- 追问 Q11a（★★★）：你说终态不可再转换，但代码里如果收到一条非法的转换请求，是抛异常还是静默忽略？为什么这么选？
  - 口播：我选的是静默忽略，不抛异常，这个决定背后是并发环境的现实考量。状态转换发生在多个入口：正常完成的回调会请求转成功，取消请求会把运行中转成取消中，替换逻辑会把旧任务置成已替换。这些入口在并发下可能几乎同时触发，比如任务刚完成的同时取消请求也到了，两个请求各自以为自己是合法的。如果非法转换抛异常，这些正常的竞态会让运行时炸出噪音错误，把合法的完成路径打断；而静默忽略配合幂等收敛，最后落到账上的必然是先到先得的一个终态，状态机不变量保持。代价是调用方无法靠异常发现自己请求被忽略，所以我在测试里对每一条合法转换和非法转换都做了断言，把"什么能转、什么会被忽略"固化成契约。如果要让外部感知被忽略的原因，可以扩展成带原因的拒绝返回值，但那会改变接口形态，我当时权衡后选择了纯函数加静默忽略的最小设计。

**Q12（★★★）可观测性你选了 fail-open，监控链路出问题不能影响主业务。反过来想，什么场景下你会改成 fail-closed？**

- 考察：取舍不是立场而是场景函数；工程判断。
- 覆盖：当前阶段观测是增强能力 → 若观测承担强制审计/合规/计费/安全责任，丢事件=丢钱丢合规 → 届时应改为可靠投递（落库+重试+确认），而不是简单 fail-closed 阻断执行。
- 口播：我当时的场景是教学与实践型系统，事件流的职责是让用户看到执行过程，属于增强体验的能力，所以选 fail-open：事件投递失败只影响可视性，绝不能反向把智能体主执行搞挂，这是可观测性的基本礼貌——监控不应该成为业务可用性的单点。但我不会把这个选择说成普适真理。如果场景变成强制审计：每笔 SQL 执行都要留痕，丢了等于合规事故；或者变成计费依据：每次工具调用都要记账，丢事件等于丢收入。这时候我会把事件链路升级为可靠投递：事件先落库或写可靠队列，再加确认与重试，投递失败要有告警和补偿，而不是简单地在执行路径上抛错阻断——因为阻断执行同样会造成更大的业务损失。所以真正的问题是"这条事件丢了，代价落在谁身上"，当前系统里代价落在"用户体验"上，所以可以宽松；将来代价落到"钱、合规、安全"上，就必须把观测从旁路提升为主链路的一部分，架构上从旁路改为主链路，这是我会向面试官说明的判断框架。
- 追问 Q12a（★★★）：你的事件目前只进 WebSocket，没有落库。如果用户刷新页面，历史事件还能看到吗？这算不算设计缺陷？
  - 口播：刷新后看不到历史执行事件，这确实是当前版本的明确边界，我不回避。事件在内存里产生、按会话推送，消费完就没了，任务虽然结束但它的状态记录还在服务端，所以用户能查"最后一次任务的成败"，但看不到完整的过程时间线。原因是我当时的优先级是把"实时可观察"做扎实，事件持久化和断线回放属于下一个阶段的方向，我在演进文档里把它登记在路线图上而不是假装已完成。如果要补，方向很明确：事件在产生时异步落库，会话的历史查询接口按批次与序号回放，前端断线重连后按最后收到的序号增量拉取，这样实时推送和事后回放就能衔接起来，这也是我设计 seq 为单调全序的原因之一——它天然可以作为断线续传的游标。回答这个问题我建议的姿态是主动承认并给出带游标的演进设计，而不是把没落库说成有意为之的极简主义。

**Q13（★★★）子智能体调用在你们这套里是怎么被识别和上报的？你理解的多智能体路由机制到什么程度？**

- 考察：不把框架黑盒当自己发明；是否知道 Orchestrator–Workers 是模式落地。
- 覆盖：DeepAgents 主智能体调度子智能体在流式输出中表现为"任务"类型的工具调用 → 按此识别并上报助手调用事件 → 路由依据是各子智能体的能力描述（提示词层）→ 明确这是 Orchestrator–Workers 模式的应用而非原创架构。
- 口播：我先说明这套编排基于 DeepAgents 框架提供的多智能体能力，我做的是架构选择与工程化，不是发明编排内核。结构是典型的主从模式：主智能体持有少量文件类工具和三个专家的"名片"，它自己决定把任务派给谁。在实现层面有个很关键的细节：框架把"调用子智能体"也表达成一次工具调用，名字是任务分发，所以我在流式输出里识别这个特定工具调用，从中取出目标助手类型和任务描述，单独上报一条"正在调用某某专家"的事件，前端就能展示"现在派给了哪个助手"。至于主智能体凭什么选对助手，路由依据是每个专家暴露的能力描述和主智能体的系统提示词，由模型判断——这是 LLM 路由，存在选错、重复选择这类模型错误，我的工程化没有消除它们，只是让整个过程可观察、可回放。我会明确说这是 Orchestrator–Workers 模式的一个应用案例，如果面试官问原创性，我会把价值定位在"模式落地 + Runtime 治理"，而不是声称设计了新的编排理论。
- 追问 Q13a（★★★）：如果主智能体把一个数据库问题错误派给了网络搜索助手，你的系统会怎么办？有没有纠错或重试机制？
  - 口播：没有自动纠错或重试机制，这是我必须诚实交代的边界。框架的流程是主智能体调用专家、专家返回结果、主智能体再决定下一步；如果第一次派错，主智能体看到专家返回的结果后理论上可以意识到不对再改派，这依赖模型的自我修正能力，但没有结构化的强制保障，比如按结果类型做路由校验、失败自动改派、重试预算这类机制都没有。我当时的设计重心在运行时治理而不是任务质量闭环：任务能不能跑完、状态准不准、过程可不可观察，这些是确定性问题，做了；任务结果质量高不高、路由对不对，属于模型层面和评测层面，路线图上登记了评测体系，但没有在版本里实现。如果面试官问生产化改进，我会说两条：一是给专家工具返回值加结构化类型，让主智能体的路由决策可以部分校验；二是建评测用例集把"路由正确率"变成可回归的指标，而不是靠肉眼观察几次演示就宣称可靠。

**Q14（★★★）状态机、取消竞态这类并发语义，你怎么证明代码是对的？确定性测试是怎么做的？**

- 考察：测试设计与工程纪律，是否理解"并发 bug 靠碰运气测不出来"。
- 覆盖：纯函数（状态机转换）直接单测矩阵；取消/替换用可注入的假执行器控制时序与结果；API 层注入假流式输出驱动真实入口，不打真实模型；外部依赖用例条件跳过；双环境回归留档。
- 口播：这类代码最怕"看起来能跑"——取消竞态可能跑一百次才错一次，手工验证等于碰运气。我的策略是把并发语义拆成能确定控制的部分。第一层是纯逻辑：状态机合法转换是纯函数，输入当前状态和目标状态输出能不能转，直接全量枚举状态对做矩阵断言，终态不可再转换也变成普通单元测试。第二层是时序控制：取消和替换的测试里注入假执行器，让假执行器按脚本走，比如"收到取消但选择继续完成"或"故意晚结束"，这样竞态不是随机发生而是被测试精确制造出来，断言收到取消信号恰好一次、宽限超时后最终由完成回调收尾、旧任务的迟到回调不覆盖新状态。第三层是 API 控制流：接口层测试注入假的流式输出源，用固定数据块驱动真实的执行入口跑完整个控制流，不打真实模型，所以测试确定且快。依赖真实外部环境的用例用条件跳过门控，本地没环境时整条测试基建照常可跑。整个体系在系统环境和锁定依赖环境两套都跑过全量回归并留档，我不会拿"手动验证过"来当证据，测试本身要能回答改坏了没有。
- 追问 Q14a（★★★）：你注入假执行器测取消，和真实模型执行时的取消行为会不一样吗？这个差距你怎么看？
  - 口播：会有差距，我分两层看。确定性测试证明的是运行时语义正确：取消信号发一次、宽限收敛、最终状态落定，这些不依赖具体执行体是什么，假执行器能验证逻辑层，这层结论对真实模型同样成立，因为运行时只看执行体是否响应取消与最终返回什么，不关心它内部是模型还是别的。真实差距在另一层：真实模型执行时，取消请求到达的时机落在模型调用、工具调用还是流式输出中间，行为细节可能不同，比如正在等模型流式响应时取消是否立刻生效，这属于模型 SDK 的取消传播行为，我受本机环境限制没有做过真实模型的端到端取消验证，这一条我如实标注为环境限制，没有把它包装成已验证。如果面试官关心，我会说明补齐路径：用真实模型跑一组取消、替换的端到端用例，把"模型调用中取消"这种真实时序纳入回归，这与假执行器的确定性测试互补，一个是逻辑正确性，一个是生态兼容性，两者不能互相替代。

**Q15（★★★）假设让你把这个系统往生产环境推，抛开部署和运维，从架构上你会优先补哪三件事？为什么？**

- 考察：全局视野、优先级判断、不吹牛。
- 覆盖：任务账本与队列的分布式化 / 事件落库与回放 / 认证与多租户隔离 / 评测体系（按价值排序并说明理由与顺序依赖）。
- 口播：我会按"先堵风险、再补能力、后建度量"的顺序选三件事。第一件事是任务状态与执行的可靠化：现在任务账本在单进程内存，事件也不落库，生产环境多实例、进程重启会直接丢失用户可查询的状态，所以先把任务记录和事件从内存搬到可靠存储，任务队列与实例化语义才谈得上，这是地基，没有它后面都悬。第二件事是认证与多租户隔离：当前没有任何登录与授权，接口任何人可调，会话目录也只有随机标识这一层软隔离，这在生产是不可接受的，至少要接入身份认证、把会话归属到用户、在文件与任务的访问路径上加归属校验，这也是所有工具安全边界真正生效的前提——没有租户概念时，沙箱防的是模型，有了租户还要防用户。第三件事是评测体系：把网络、数据库、知识库、文件分析几类任务的用例集和离线执行器建起来，让"路由对不对、报告质量稳不稳定"成为可回归的指标，否则 Agent 系统的行为改进没有衡量手段，越改越不敢动。事件审计、链路追踪这些我会排在更后面，因为它们依赖前两件事先立起来。三件事的顺序是有依赖的：可靠性地基 → 安全与租户 → 质量度量。

## 4. 源码证据索引

> 项目根 = 仓库 `E:\CodeField\deepsearch-agents`。R4 相关文件为工作区未提交改动（`git status` 可见）。

| 主题 | 关键路径与内部符号 | 对应正文位置 |
| --- | --- | --- |
| 任务生命周期状态机 | `app/runtime/task_manager.py`：`TaskStatus`（五态枚举）、`_LEGAL_TRANSITIONS`、`_TERMINAL_STATUSES`、`can_transition()`、`TaskRecord`、`RunOutcome`、`TaskManager.submit/cancel/status/_finalize_from_done/_apply_transition`、`_outcome_to_terminal()`、`TransitionHook` | Q2 / Q6 / Q11 / bullet1-2 |
| 取消与替换一致性 | `task_manager.py` 的 `cancel()`：`task.cancel()` → `await asyncio.wait_for(asyncio.shield(task), cancel_grace_seconds)`；`_finalize_from_done()` 的 identity 校验 `self._tasks.get(thread_id) is task`；`_tasks`/`_records` 双表分离；`submit()` 里旧记录置 `CANCELLED(detail="replaced")` | Q3 / Q7 / bullet2 |
| 统一事件模型与有序投递 | `app/api/monitor.py`：`RunTerminalGuard`（`TERMINALS`/`may_emit`）、`ToolMonitor` 单例（`_seq`+`_seq_lock`、`_emit()` 信封 `event_id/run_id/thread_id/seq/timestamp`、`report_tool/report_assistant/report_task_started/report_task_result/report_task_cancelled/report_error/report_session_dir`）、`ConnectionManager`（per-thread `asyncio.Queue`+`_consumer`、`enqueue` 跨线程 `call_soon_threadsafe`、fail-open） | Q5 / Q5a / Q11 / Q12 / bullet3 |
| 会话与批次上下文 | `app/api/context.py`（thread_id/session_dir 的 ContextVar 与 Token reset）；`monitor.py` 顶部 `_run_id_ctx`/`set_run_context` | Q9 / bullet3 |
| 执行入口与终态返回 | `app/agent/main_agent.py`：`run_deep_agent()`、`monitor.report_*` 调用点、`terminal_guard`、`astream` 循环、会话目录与 `restore_uploads_to_session`、finally 中 reset 上下文、返回 `RunOutcome` | Q1 / Q6 / bullet1-3 |
| Checkpoint 持久化 | `app/runtime/checkpoint.py`：`AsyncBridgeSqliteSaver`（`aget_tuple/alist/aput/aput_writes` 桥接 `asyncio.to_thread`）、`AGENT_CHECKPOINT_DB`/默认 `app/runtime/checkpoints.sqlite`、按路径单例 `_savers`+`_lock`、`get_sqlite_checkpointer()`、fail-fast `RuntimeError` | Q8 / Q8a / bullet4 |
| 路径沙箱 | `app/utils/path_utils.py`：`PathSafetyError`、`is_within_directory`、`resolve_path`（虚拟前缀/嵌套路径清洗 + 会话目录包含性校验）、`_reject_parent_traversal` | Q4 / bullet5 |
| 上传治理 | `app/utils/upload_guard.py`：`UploadRejectedError`、`sanitize_filename`、`validate_extension`、`validate_size`、`generate_storage_name`、`is_storage_name`、`load/save_upload_manifest`、`restore_uploads_to_session` | Q4 / Q10 / Q10a / bullet5 |
| 会话标识净化 | `app/utils/session_id.py`：`InvalidThreadIdError`、`is_safe_thread_id`、`sanitize_thread_id`、`safe_thread_id_or_new` | Q4 / bullet5 |
| Text-to-SQL 安全 | `app/utils/sql_security.py`：`READ_ONLY_KEYWORDS`、`_tokenize`、`validate_read_only_sql`、`extract_table_names`、`validate_allowed_tables`、`validate_params`、`prepare_read_only_query`；`app/tools/db_tools.py`：`_audit_sql`、`fetch_allowed_tables`、`list_sql_tables`、`get_table_data`、`execute_sql_query` | Q4a / bullet5 |
| 工具归口与编排 | `app/agent/main_agent.py` 顶部 `create_deep_agent(...)`（tools 只挂文件类、subagents 三专家）；`app/agent/subagents/{database_query_agent,network_search_agent,knowledge_base_agent}.py`；`app/prompt/prompts.yml`；流式输出中识别名为 `task` 的工具调用上报助手事件 | Q1 / Q13 / bullet1 |
| API 装配 | `app/api/server.py`：`run_task`、`cancel_task`、`task_status`、`upload_files`、`download_file`、`list_files`、`websocket_endpoint`；`lifespan` 装配 `TaskManager(runner=run_deep_agent)` | Q1 / Q14 / bullet1-2 |
| 测试证据 | `tests/test_task_manager.py`（转换矩阵/CancelledError 计数/替换一致性）、`tests/test_task_api.py`（假 runner 注入）、`tests/test_agent_events.py`、`tests/test_checkpoint_recovery.py`（真实子进程恢复）、`tests/test_db_tools*.py`、`tests/test_path_utils.py`、`tests/test_session_id.py`、`tests/test_upload_guard.py` | Q14 / Q14a / bullet6 |
| 设计与问题记录 | `docs/spec/2026-09-03-R{1,2,3,4}-*.md`、`docs/problem/P001–P006`、`docs/ROADMAP.md`、`ARCHITECTURE.md`、`DECISION.md`（D004/D007/D009 相关决策） | 全部（演进叙事） |

## 5. 附录：简历表述边界与仓库事实核对（防问穿）

> 依据仓库代码与文档逐条核对；写简历或口播时遵守以下边界，避免与事实冲突。

1. **技术栈如实**：本项目不包含 FAISS / Redis / 自建向量库 / 自建检索；RAGFlow 是经 SDK 接入的**外部私有知识库服务**，只是三路检索来源之一，系统**不基于任何开源引擎改造**（`pyproject.toml`、`app/tools/ragflow_tools.py` 可证）。简历技术栈若写"LangChain"，请同时能说出 DeepAgents/LangGraph 在其中的角色（框架提供多智能体执行器，LangChain 提供工具与模型抽象，LangGraph 提供图执行与 Checkpoint 语义）。
2. **不写"企业级/生产级/多租户/服务 N 个部门/权限控制"**：仓库 P005 明确登记无认证、无授权（教学边界）；会话隔离是 thread_id + ContextVar + 独立工作目录，不等于多用户权限体系。
3. **不把未实现当成果**：任务超时自动 FAILED(timeout)、自动重试、分布式任务队列、事件落库与历史回放、跨会话长期记忆（BaseStore）、多实例 checkpoint、链路追踪（trace_id 贯穿）、工具 completed/failed 事件均为后续阶段，口播中若主动提及只能是"演进方向"。
4. **R4 是工作区未提交代码**：`task_manager.py`、`tests/test_task_manager.py`、`tests/test_task_api.py`、R4 spec 目前 untracked/modified。面试若被问"是否上线/合入"，如实说"实现与测试完成于工作区，等待整理提交"，不要声称已发布。
5. **指标口径**：README 记录上传安全 80+ 用例、SQL 安全 86 用例；R4 交付验收留档 209 passed / 8 skipped（系统环境）与 220 passed / 0 skipped（uv 锁定环境，含真实 MySQL 集成）。这些是测试数量不是线上指标；无 QPS/延迟/Token 节省/用户量数据。面试前建议重跑 `uv run pytest -q` 复核。
6. **已知真实边界（主动交代加分）**：取消对同步阻塞的第三方调用是尽力而为；旧任务被替换后其事件仍会发出（前端需按批次过滤，未做收尾事件）；事件不落库、刷新丢历史；SQL 校验是词法级、非完整 SQL 语义解析，生产需只读 DB 账号兜底；真实 LLM 下的取消/替换端到端未验证（环境限制，确定性测试用假执行器）。


