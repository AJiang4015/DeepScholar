# 简历项目经历包装稿：深度研搜（deepsearch-agents）

> 定位：教学/实践型多智能体深度研究 Demo。所有表述均基于仓库真实代码，可逐条回查。
> 核心文件索引：`app/agent/main_agent.py`、`app/api/server.py`、`app/api/monitor.py`、`app/api/context.py`、`app/tools/*.py`、`app/utils/path_utils.py`、`app/utils/word_converter.py`、`app/prompt/prompts.yml`、`frontend/src/hooks/useDeepAgentSession.ts`

---

## 第一部分：项目定位

这是一个教学/实践型的多智能体深度研究系统 Demo，本质是"把一次普通问答升级为一次深度研究任务"的 Agent 工程主链路。在 Java 后端简历中，它不应作为"AI 平台"出现，而应定位为**以后端工程能力为主干、Agent 编排为业务场景的项目载体**：异步服务、WebSocket 事件驱动、会话隔离、任务生命周期、数据库工具链、文件访问边界这些能力可直接迁移到 Java 技术栈，Agent 部分则体现对 AI 应用开发的理解。项目经历表述为"理解整体架构后，独立完成核心模块复刻、调试与部分工程化改造"。

---

## 第二部分：推荐项目名称（5 选 1）

1. **深度研搜：可观测的多智能体深度研究服务**（推荐，突出事件驱动与可观测性）
2. **DeepSearch：基于异步任务与 WebSocket 的多智能体研究系统**
3. **对话式多智能体研究助手（Orchestrator-Workers 实践）**
4. **多智能体深度研搜平台：会话隔离与任务生命周期管理**
5. **深度研搜 Demo：从多来源检索到文件交付的 Agent 主链路**

选名原则：不出现"生产级 / 企业级 / 平台级"字眼；用"服务 / 系统 / Demo"体现工程感；保留"多智能体"关键词匹配 AI 应用岗位。

---

## 第三部分：技术栈

- **后端**：Python 3.12 / FastAPI / Uvicorn / asyncio / ContextVar / Pydantic
- **Agent**：DeepAgents 0.5.7 / LangGraph（Checkpointer）/ LangChain / OpenAI 兼容大模型 / Tavily / RAGFlow SDK
- **数据库**：MySQL 8.4（Docker Compose 部署）/ mysql-connector-python
- **通信**：WebSocket（心跳保活、断线重连、跨事件循环线程安全投递）
- **前端**：React / Vite / TypeScript / Ant Design
- **工程化**：uv / pnpm / pre-commit（ruff）/ dotenv 配置管理

> 说明：Tavily / RAGFlow / DeepAgents 属 Agent 生态名词，写在技术栈即可；简历正文不要以"我会 LangGraph/Tavily"为主语，而要以"我实现了 XX 后端能力"为主语。

---

## 第四部分：最终简历项目经历（4~6 条）

> 每条 =【方案】+【实现】+【解决的问题】，不虚构数字，不用夸大词。

1. **异步任务与任务生命周期管理**：基于 FastAPI + asyncio 构建研究任务接口，将长耗时 Agent 执行放入后台 Task（`asyncio.create_task`），HTTP 接口立即返回；通过 `active_tasks` 注册表维护任务创建、状态跟踪与 done_callback 自动清理，取消请求采用 cancel + 超时返回 cancelling 中间态，避免接口阻塞和会话目录并发写入。

2. **WebSocket 事件驱动的执行过程可观测**：将 Agent 执行抽象为统一事件流（会话创建、工具调用、助手分派、任务结果、异常、取消），按 `thread_id` 定向推送；后台协程通过绑定主事件循环，区分同循环 `create_task` 与跨线程 `run_coroutine_threadsafe` 两种投递路径，实现长任务执行过程实时可见。

3. **协程级会话隔离（ContextVar）**：利用 `ContextVar` 保存 `thread_id` 与 `session_dir`，配合 Token reset 防止上下文残留；每个会话独立 `output/session_xxx` 工作目录，深层工具无需层层传参即可经 `get_session_context()` 获取会话目录，避免异步并发请求间串台。

4. **多智能体编排（Orchestrator-Workers）**：主智能体仅持有文件交付类工具，三个专家子智能体分别负责互联网检索（Tavily）、结构化查询（MySQL）、私有文档问答（RAGFlow），通过能力描述（description）由主智能体路由分派，按职责划分工具权限，降低单 Agent 的工具选择复杂度。

5. **LLM 文件访问边界（路径沙箱）**：设计统一路径解析层，将模型侧虚拟路径（`/workspace` 等）与相对路径清洗回当前会话目录，消除重复嵌套路径；文件列表/下载接口通过 `resolve` + `is_relative_to` 校验目录归属，将文件访问范围限制在会话输出目录内，防止目录穿越。

6. **Text-to-SQL 工具链与文件交付闭环**：将数据库访问拆分为列表、数据预览（前 100 行）、SQL 执行三个工具，并通过提示词约束"先确认表结构再执行查询"的调用顺序，降低 LLM 直接生成 SQL 的错误率；支持读取多格式附件（md/txt/docx/pdf/xlsx）并生成 Markdown、转换为中文 PDF，完成从检索到交付的完整链路。

---

## 第五部分：面试追问地图

### Bullet 1（异步任务生命周期）
- **可能追问**：为什么用 `create_task` 而不是直接 `await`？为什么接口要立即返回？`task.cancel()` 的原理？取消后任务一定会停吗？
- **必须掌握**：长任务直接 await 会阻塞请求直到完成；create_task 将协程交给事件循环调度，接口秒回；cancel 向协程注入 `CancelledError`；代码 `server.py` 用 `asyncio.wait_for(task, timeout=1.0)` 区分 cancelled / cancelling / 异常三态；`_forget_task` 用"仍指向同一 task 才删除"避免误清同 thread_id 的新任务。
- **最易被问穿**：声称"实现了可靠任务取消"——底层同步阻塞调用无法中断（代码注释 `server.py` 自己承认）。
- **推荐回答**：先讲注册表 + done_callback + 三态设计的合理性，再主动说"取消对同步阻塞的第三方调用是尽力而为，这是当前版本边界"。

### Bullet 2（WebSocket 事件推送）
- **可能追问**：为什么要有两种投递方式？`create_task` 和 `run_coroutine_threadsafe` 的区别？为什么 WS 必须在创建它的事件循环里发消息？Agent 任务跑在哪个循环？
- **必须掌握**：FastAPI 的 WebSocket 对象与创建它的 loop 亲和；`lifespan` 里 `manager.set_loop(loop)` 绑定主循环；`monitor._send_to_websocket` 判断当前 loop 是否等于主 loop，同循环直接 `create_task`，跨线程（如 run_in_executor 场景）用 `run_coroutine_threadsafe` 线程安全投递。
- **最易被问穿**：说不清"为什么需要绑定 loop"——答不出 asyncio 的 loop 亲和性与线程安全问题。
- **推荐回答**：画出执行流（HTTP → create_task → astream → monitor → manager.send_to_thread），强调"事件按 thread_id 定向推送、不在同一个 loop 时用线程安全方式投递"。

### Bullet 3（ContextVar 会话隔离）
- **可能追问**：为什么不用全局变量？ContextVar 和 ThreadLocal 区别？协程并发时会不会串 session？为什么需要 reset？session_dir 怎么传到深层工具？
- **必须掌握**：全局变量在并发请求下串台；ContextVar 是协程级上下文，asyncio 单线程内协程切换也互不干扰，而 ThreadLocal 按线程共享会串；`set_session_context` 返回 Token，`finally` 中 `reset` 防止本次会话信息残留到后续请求；工具内 `get_session_context()` 直接读取，无需逐层传参。
- **最易被问穿**：把 ContextVar 说成"线程隔离"或"全局状态"。
- **推荐回答**：以"为什么不用全局变量"切入，讲清协程级 vs 线程级 vs 全局三级对比，再讲 token reset 的必要性。

### Bullet 4（多智能体编排）
- **可能追问**：为什么子智能体用字典定义？description 怎么参与路由？为什么文件工具只给主智能体？子智能体之间能互相调用吗？
- **必须掌握**：`create_deep_agent(..., subagents=[...])` 接收字典式子智能体（`subagents/*.py`），`description` 是主智能体判断"该把任务派给谁"的依据（`prompts.yml`）；文件工具（read/generate/convert）只挂主智能体，因为交付职责单一、避免子智能体乱写文件；DeepAgents 中主智能体调用子智能体在流式输出中表现为名为 `task` 的工具调用（`main_agent.py` 据此上报 assistant_call 事件）。
- **最易被问穿**：把"Orchestrator-Workers 模式的应用"说成"原创架构"。
- **推荐回答**：明确这是该模式的落地，讲清职责划分动机（信息获取 vs 文件交付分离，降低单 Agent 工具面）。

### Bullet 5（LLM 文件访问边界）
- **可能追问**：为什么模型产生的路径不能直接用于文件系统？resolve_path 处理了哪些情况？is_relative_to 防什么？还有哪些漏洞？
- **必须掌握**：模型常返回 `/workspace`、`/mnt/data` 等虚拟路径、重复嵌套路径（`session_xxx/session_xxx/`）；`path_utils.resolve_path` 剥离虚拟前缀、去重嵌套、统一解析回当前会话目录；`server.py` 下载/列表接口 `Path(path).resolve()` 后再 `is_relative_to(output_abs)` 防止 `../` 目录穿越。
- **最易被问穿**：夸大成"完整文件系统安全体系"——上传无类型白名单、无病毒扫描、下载接口暴露绝对路径。
- **推荐回答**：限定措辞为"访问边界约束（path sandbox）"，主动说"这解决的是模型路径不可信问题，不等于生产级安全体系"。

### Bullet 6（Text-to-SQL 与文件交付）
- **可能追问**：为什么拆三个工具？顺序约束在哪里？SQL 拼接安全吗？怎么防止模型写坏 SQL？PDF 中文怎么解决？
- **必须掌握**：拆分让模型先见 schema 再写 SQL，顺序约束同时写在工具 docstring（`db_tools.py`）与提示词（`prompts.yml`）里；SQL 为直接拼接（表名、语句），代码注释自己承认生产环境需白名单校验，**这是注入风险不是安全设计**；PDF 用 ReportLab + UnicodeCIDFont(STSong-Light) 自研 Markdown 子集解析（标题/表格/代码块/行内加粗）。
- **最易被问穿**：说"实现了 SQL 安全"或"SQL 防注入"。
- **推荐回答**：讲清三段式工具链的工程动机（降低 LLM 出错率），主动承认拼接风险并给出改进方向（白名单 + 参数化 + 只读模式）。

---

## 第六部分：项目局限与防守话术

> 统一回答句式："这是当前版本的设计边界，我当时优先解决的是 XXX。如果继续往生产环境演进，我会采用 XXX。"

1. **没有认证与权限体系** → 当前实现：无登录、无鉴权，任何人可调 `/api/task` → 为什么：教学项目优先跑通主链路 → 局限：无法多用户隔离 → 演进：加 API Key 中间件或 JWT 登录 + 用户级会话目录归属。
2. **CORS 配置不合理** → 当前：`allow_origins=["*"]` + `allow_credentials=True` → 为什么：本地前后端分离联调方便 → 局限：生产不安全 → 演进：收敛为白名单域名并拆分环境配置。
3. **SQL 工具存在拼接注入风险** → 当前：`execute_sql_query`/`get_table_data` 直接拼接表名与语句 → 为什么：教程演示链路优先 → 局限：模型可能生成任意 SQL → 演进：工具层只放行 SELECT/SHOW + 表名白名单 + 参数化查询 + 查询审计（见第七部分改造 1）。
4. **上传缺少完整治理** → 当前：仅按会话目录落盘，无大小/类型限制 → 为什么：Demo 场景 → 局限：恶意文件或超大文件风险 → 演进：类型白名单、大小上限、文件名清洗/随机化（见改造 2）。
5. **没有真正的长期记忆** → 当前：仅 `InMemorySaver` 同会话内复用上下文 → 为什么：演示 thread_id 会话能力 → 局限：进程重启即丢 → 演进：换 SQLite/Postgres Checkpointer（见改造 3）。
6. **InMemorySaver 进程重启丢失** → 同 5，补充：这是"会话级状态复用"，不是"持久化记忆"，简历不要写后者。
7. **文件列表是轮询不是推送** → 当前：前端 `setInterval` 2.5s/6s 轮询 `/api/files` → 为什么：实现简单 → 局限：实时性差、有无效请求 → 演进：服务端在文件生成后主动推 `file_created` 事件，前端改为事件驱动刷新。
8. **任务取消无法中断同步阻塞调用** → 当前：cancel 注入 CancelledError，但底层第三方同步调用要等返回 → 为什么：异步模型对阻塞调用天然受限 → 局限：取消可能延迟生效 → 演进：工具层提供可中断信号/子进程隔离，或把阻塞调用丢线程池并配合 future.cancel。
9. **没有测试体系** → 当前：仓库无 pytest，仅 pre-commit 格式检查 → 为什么：教程项目优先演示功能 → 局限：回归靠手测 → 演进：为 server 接口、path_utils、db 工具补第一批 pytest（见改造 3 可选延伸）。
10. **active_tasks 是单进程内存态** → 当前：进程内 dict，非分布式 → 为什么：单机 Demo → 局限：无法横向扩展 → 演进：引入 Redis 任务存储 + Celery/ARQ 队列，或多实例 + 外部调度。
11. **没有任务队列** → 同 10，补充：当前是"每会话单任务 + 新任务取消旧任务"策略，不是队列 → 演进：asyncio.Queue 限流 + 优先级 + 超时熔断。
12. **没有生产级审计体系** → 当前：事件只在内存与 WebSocket 流转，未落库 → 为什么：优先做"可观察"而非"可审计" → 局限：无法追溯历史 → 演进：monitor 事件异步持久化到 DB，提供历史会话回放接口。

---

## 第七部分：建议真正补做的 3 个改造

> 选择标准：修改量小、面试价值高、容易证明是自己做的、能把"坑"变成"亮点"。

### 改造 1：SQL 工具安全加固（首选）
- **修改文件**：`app/tools/db_tools.py`（核心）、`app/utils/`（可加一个 sql 白名单模块）、补 1 个 pytest。
- **修改思路**：① `execute_sql_query` 前置校验：只放行 SELECT/SHOW/DESC/EXPLAIN 开头；② 表名白名单：解析 SQL 中引用的表名，与 `list_sql_tables` 结果比对，未命中即拒绝；③ 参数化：如支持条件注入则改用参数占位；④ 记录每次执行的 SQL 到日志（审计）。
- **预计工作量**：0.5~1 天。
- **简历怎么写**："对 text-to-SQL 工具链进行安全加固：增加只读语句校验与表名白名单，阻断模型越权写库路径，并补充查询审计日志。"
- **面试怎么讲**："教程版是直接拼接 SQL，我接手后第一件事就是封住注入面——只读白名单 + 表名校验 + 审计，讲清校验逻辑（正则/词法解析表名）和残留边界（未做 SQL 语义级解析）。"

### 改造 2：上传文件治理
- **修改文件**：`app/api/server.py`（upload 接口）、`app/utils/`（新增 upload 校验函数）。
- **修改思路**：① 类型白名单（md/txt/docx/pdf/xlsx/xls + MIME 校验）；② 单文件与总大小上限（如 20MB/100MB）；③ 文件名清洗：去路径分隔符、防 `../`、重复名加后缀或改随机 UUID 存储；④ 上传结果返回清洗后的文件名，与后续读取链路对齐。
- **预计工作量**：0.5 天。
- **简历怎么写**："完善文件上传治理：增加类型白名单、大小上限与文件名清洗，防止路径穿越与超限文件进入会话工作目录。"
- **面试怎么讲**："上传是模型文件沙箱的第一道门，我先用白名单和大小限制挡住明显风险，再用文件名清洗保证落盘路径可控；对比改造前后，说明清洗规则（拒绝路径分隔符、防目录穿越）。"

### 改造 3：Checkpointer 持久化（InMemorySaver → SQLite）
- **修改文件**：`app/agent/main_agent.py`（一处构造）、`pyproject.toml`（加 langgraph-checkpoint-sqlite）、`.env.example`。
- **修改思路**：把 `InMemorySaver()` 换成 `SqliteSaver`（或 PostgresSaver），按 thread_id 复用执行状态；进程重启后同一 thread_id 可恢复上下文；同时把 `active_tasks` 的清理逻辑与恢复流程对齐（可选：加一个"恢复会话"接口）。
- **预计工作量**：0.5 天（SQLite 版）。
- **简历怎么写**："将会话状态存储从内存 Checkpointer 升级为 SQLite 持久化，同一 thread_id 的执行上下文在服务重启后仍可恢复，为会话续跑与历史回放打基础。"
- **面试怎么讲**："原版进程一重启会话就丢，我改用 SQLite Checkpointer 把状态落盘；讲清 Checkpoint 存什么（图状态/消息）、thread_id 如何索引、为什么选 SQLite（零部署）以及升级 Postgres 的路径。"

> 备选：事件持久化（monitor 落库 + 历史回放）、API Key 认证中间件、pytest 首批测试——三者可任意替换上面任意一项，工作量都约 0.5~1 天。

---

## 第八部分：最终推荐版本（可直接复制）

---

**深度研搜：可观测的多智能体深度研究服务（FastAPI + DeepAgents）**

基于多智能体编排的深度研究应用 Demo：用户提交研究任务后，系统自动完成任务规划、多来源信息检索（互联网 / 结构化数据库 / 私有文档库）、结果汇总与文件交付（Markdown / PDF），并将执行过程实时推送到前端。项目作为 Agent 工程主链路的完整实践，重点沉淀异步服务、事件驱动、会话隔离等后端工程能力。

**技术栈**：Python 3.12 / FastAPI / asyncio / WebSocket / ContextVar / DeepAgents / LangGraph / LangChain / MySQL 8.4 / Tavily / RAGFlow SDK / React / TypeScript / Vite / uv / Docker Compose

**核心项目经历**：

1. **异步任务与生命周期管理**：基于 FastAPI + asyncio 将长耗时 Agent 执行放入后台 Task，接口立即返回；通过任务注册表维护创建、状态跟踪与 done_callback 清理，取消请求采用 cancel + 超时返回 cancelling 中间态，避免接口阻塞与会话目录并发写入。

2. **WebSocket 事件驱动的执行可观测**：将 Agent 执行抽象为统一事件流（会话创建、工具调用、助手分派、结果、异常、取消），按 thread_id 定向推送；后台协程区分同循环 create_task 与跨线程 run_coroutine_threadsafe 两种投递路径，实现长任务执行过程实时可见。

3. **协程级会话隔离**：利用 ContextVar 保存 thread_id 与 session_dir，配合 Token reset 防止上下文残留；每会话独立工作目录，深层工具经 get_session_context() 获取会话信息，避免异步并发请求串台。

4. **多智能体编排（Orchestrator-Workers）**：主智能体仅持有文件交付工具，三个专家子智能体分别负责互联网检索、结构化查询、私有文档问答，通过能力描述实现路由与工具权限划分，降低单 Agent 工具选择复杂度。

5. **LLM 文件访问边界**：统一路径解析层将模型侧虚拟路径与相对路径清洗回会话目录；列表/下载接口经 resolve + is_relative_to 校验目录归属，限制文件访问范围并防止目录穿越。

6. **Text-to-SQL 工具链与文件交付**：数据库访问拆分为列表、预览、执行三个工具，以提示词约束调用顺序，降低 LLM 生成 SQL 的错误率；支持多格式附件读取与 Markdown/中文 PDF 生成，打通从检索到交付的完整链路。

---
